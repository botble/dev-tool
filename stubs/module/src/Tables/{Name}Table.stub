<?php

namespace {Module}\Tables;

use {Module}\Models\{Name};
use Triyaq\Table\Abstracts\TableAbstract;
use Triyaq\Table\Actions\DeleteAction;
use Triyaq\Table\Actions\EditAction;
use Triyaq\Table\BulkActions\DeleteBulkAction;
use Triyaq\Table\BulkChanges\CreatedAtBulkChange;
use Triyaq\Table\BulkChanges\NameBulkChange;
use Triyaq\Table\BulkChanges\StatusBulkChange;
use Triyaq\Table\Columns\CreatedAtColumn;
use Triyaq\Table\Columns\IdColumn;
use Triyaq\Table\Columns\NameColumn;
use Triyaq\Table\Columns\StatusColumn;
use Triyaq\Table\HeaderActions\CreateHeaderAction;
use Illuminate\Database\Eloquent\Builder;

class {Name}Table extends TableAbstract
{
    public function setup(): void
    {
        $this
            ->model({Name}::class)
            ->addHeaderAction(CreateHeaderAction::make()->route('{-name}.create'))
            ->addActions([
                EditAction::make()->route('{-name}.edit'),
                DeleteAction::make()->route('{-name}.destroy'),
            ])
            ->addColumns([
                IdColumn::make(),
                NameColumn::make()->route('{-name}.edit'),
                CreatedAtColumn::make(),
                StatusColumn::make(),
            ])
            ->addBulkActions([
                DeleteBulkAction::make()->permission('{-name}.destroy'),
            ])
            ->addBulkChanges([
                NameBulkChange::make(),
                StatusBulkChange::make(),
                CreatedAtBulkChange::make(),
            ])
            ->queryUsing(function (Builder $query) {
                $query->select([
                    'id',
                    'name',
                    'created_at',
                    'status',
                ]);
            });
    }
}
